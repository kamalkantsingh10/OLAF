# Story 4.1: Test Waveshare Adapter with SCS0009 Servos

## Status: Done

## Story

**As a** builder,
**I want** Waveshare adapter controlling SCS0009 ear servos,
**so that** I can verify communication with smaller servos.

## Acceptance Criteria

1. Second Waveshare Bus Servo Adapter (A) connected via USB
2. Power connected (5V from 36V→5V buck converter)
3. 4× SCS0009 servos daisy-chained (IDs 1-4)
4. Python test script moves all servos
5. Servo feedback readable

## Tasks / Subtasks

- [x] Task 1: Connect Waveshare adapter for ears (AC: 1)
  - [x] Connect second Waveshare Bus Servo Adapter (A) via USB to Pi
  - [x] Verify device appears: `ls /dev/ttyUSB*` or `ls /dev/ttyACM*`
  - [x] Verify `/dev/waveshare_ears` symlink exists (from Story 0.6)
  - [x] Test serial port can be opened via scservo_sdk PortHandler

- [x] Task 2: Connect power to Waveshare adapter (AC: 2)
  - [x] Connect 5V from 36V→5V buck converter to Waveshare adapter power input
  - [x] Verify power LED on Waveshare adapter illuminates
  - [x] Measure voltage at servo connector (~5.2-5.6V confirmed via servo voltage feedback)

- [x] Task 3: Assign servo IDs and daisy-chain (AC: 3)
  - [x] **NOTE**: IDs changed to 4-7 (not 1-4) to avoid factory default conflicts
  - [x] Connect servos one at a time, assign IDs via `ears_calibration.py set-id`
  - [x] ID 4: Left ear pan, ID 5: Left ear tilt, ID 6: Right ear pan, ID 7: Right ear tilt
  - [x] Daisy-chain all 4 servos on single bus
  - [x] Verify all 4 servos detected with `ears_calibration.py ping`
  - [x] Verified IDs persist through power cycle

- [x] Task 4: Create Python test script (AC: 4, 5)
  - [x] Created `ears_calibration.py` (calibration CLI) and `ears_servo_driver.py` (driver) in `head_ears_driver` package (not `tools/diagnostics/` — kept with driver for import sharing)
  - [x] Implement servo ping function (calibration: `ping` command)
  - [x] Implement position read function (driver: `read_position`)
  - [x] Implement position write function with speed control (driver: `move` with `speed_pct`)
  - [x] Test moving each servo to center position (driver: `center_all`)
  - [x] Test moving each servo through range (tested 5, 20, 45 degrees + limit clamping)
  - [x] Verify smooth motion without jerking (base_speed=300 confirmed smooth)

- [x] Task 5: Verify servo feedback (AC: 5)
  - [x] Read current position from each servo (driver: `read_position`, accuracy within ~4 degrees)
  - [x] Read load/torque from each servo (driver: `read_load`, confirmed non-zero under movement)
  - [x] Read temperature (driver: `read_temperature`, reads 23C — SCS0009 supports it)
  - [x] Read voltage (driver: `read_voltage`, reads 5.2-5.6V)
  - [x] Feedback test results documented in Dev Agent Record below

## Dev Notes

### Hardware Configuration

**Waveshare Bus Servo Adapter (A):**
- USB mode (not standalone mode)
- Device: `/dev/waveshare_ears` (symlink created in Story 0.6)
- Power input: 5V DC (from 36V→5V buck converter)

**Feetech SCS0009 Servos:**
- Quantity: 4× (2 per ear)
- Protocol: SCS (Serial Control System) - half-duplex UART
- Baud rate: 1000000 (1Mbps) default
- Position range: 0-1023 (10-bit)
- Voltage: 4.8-7.4V (using 5V - minimal load on ears)
- Torque: 1.6 kg·cm @ 6V

**Servo ID Assignments:**
| ID | Location | Function |
|----|----------|----------|
| 1 | Left ear | Base rotation |
| 2 | Left ear | Ear angle |
| 3 | Right ear | Base rotation |
| 4 | Right ear | Ear angle |

### Feetech SCS Protocol

**Packet format:** `0xFF 0xFF ID LENGTH INSTRUCTION PARAMS... CHECKSUM`

**Key instructions:**
- 0x01: PING - Check if servo responds
- 0x02: READ - Read register data
- 0x03: WRITE - Write register data
- 0x04: REG_WRITE - Buffered write
- 0x05: ACTION - Execute buffered writes
- 0x06: RESET - Reset to factory defaults
- 0x83: SYNC_WRITE - Write to multiple servos

**Key registers:**
| Address | Name | R/W | Description |
|---------|------|-----|-------------|
| 0x03 | ID | RW | Servo ID (1-253) |
| 0x04 | Baud Rate | RW | 0-7 (0=1Mbps) |
| 0x2A | Goal Position L | W | Target position low byte |
| 0x2B | Goal Position H | W | Target position high byte |
| 0x38 | Present Position L | R | Current position low byte |
| 0x39 | Present Position H | R | Current position high byte |
| 0x3C | Present Load L | R | Current load low byte |
| 0x3D | Present Load H | R | Current load high byte |

### Python Library

Use `feetech-servo-sdk` (installed via Poetry in Story 0.6):
```python
from scservo_sdk import *

# Initialize port
port = PortHandler('/dev/waveshare_ears')
port.openPort()
port.setBaudRate(1000000)

# Packet handler for SCS protocol
packet = PacketHandler(0)  # Protocol version 0 for SCS

# Ping servo
result, error = packet.ping(port, 1)  # Ping ID 1

# Read position
position, result, error = packet.read2ByteTxRx(port, 1, 0x38)

# Write position (move to center)
result, error = packet.write2ByteTxRx(port, 1, 0x2A, 512)
```

### Source Tree

```
tools/diagnostics/
├── test_ears_servos.py       # Test script for this story
└── test_waveshare_servo.py   # Existing general test (Story 0.6)

modules/ears/
├── README.md                 # Module documentation
└── (firmware not needed - Pi direct control)
```

### Dependencies

- Story 0.6: Configure USB Serial for Waveshare Adapters (must be complete)
- `feetech-servo-sdk` Python package (installed in project venv)

### Testing

**Manual Tests:**
```bash
# Verify device exists
ls -la /dev/waveshare_ears

# Run diagnostic script
python tools/diagnostics/test_ears_servos.py

# Expected output:
# Servo 1: PING OK, Position: 512, Load: 0
# Servo 2: PING OK, Position: 512, Load: 0
# Servo 3: PING OK, Position: 512, Load: 0
# Servo 4: PING OK, Position: 512, Load: 0
```

**Validation:**
- All 4 servos respond to ping
- Servos move smoothly through full range
- Position feedback matches commanded position (±5 counts)
- No communication errors during 100 consecutive operations

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-02-07 | 1.0 | Initial story creation for Epic 4 | Sarah (PO Agent) |
| 2026-02-07 | 1.1 | Updated power to 5V, added servo ID assignment safety warning | Sarah (PO Agent) |
| 2026-02-10 | 2.0 | Implementation complete — all tasks done, feedback verified on hardware | Dev Agent (Claude Opus 4.6) |

---

## Dev Agent Record

### Agent Model Used
Claude Opus 4.6 (claude-opus-4-6)

### Debug Log References
- SCS0009 EEPROM offset register (addr 31-32) writes persist but do NOT affect ReadPos or goal position — abandoned in favor of software center_position approach
- pip `feetech-servo-sdk` is stripped (missing scscl/sms_sts modules) — vendored full SDK at `libs/scservo_sdk/`
- Servo direction tuning: left_pan direction=1 (increasing counts=outward), right_pan direction=-1 (decreasing counts=outward), both tilts direction=1

### Completion Notes List
- Servo IDs changed from story's 1-4 to 4-7 to avoid factory default ID 1 conflicts on daisy chain
- Scripts placed in `head_ears_driver` package (not `tools/diagnostics/`) for shared imports with driver
- Center positions calibrated via software (config/servo-ids.yaml) not EEPROM offset
- Driver includes angle limits, clamping, direction control, and ±30% speed adjustment for expressions
- Feedback test results (at rest): position ~0deg (±0.6), load=0, voltage=5.2-5.6V, temperature=23C
- Feedback test results (under movement): position within ~4deg of target, load=195-1204 (non-zero confirms load sensing works)

### File List
- `ros2/src/olaf_drivers/head_ears_driver/head_ears_driver/ears_calibration.py` — NEW: Calibration CLI (ping, assign-all, set-id, set-center, go-center)
- `ros2/src/olaf_drivers/head_ears_driver/head_ears_driver/ears_servo_driver.py` — NEW: Driver with move, read_position, read_load, read_voltage, read_temperature
- `config/servo-ids.yaml` — NEW: Central servo config (IDs, limits, centers, speed, direction)
- `libs/scservo_sdk/` — NEW: Vendored full Feetech scservo_sdk (8 files)
- `pyproject.toml` — MODIFIED: Added vendored SDK package, removed feetech-servo-sdk pip dep
- `orchestrator/__init__.py` — NEW: Placeholder to unblock poetry install
- `docs/guide/ear-servo-calibration.md` — NEW: Calibration guide for contributors
- `.claude/commands/BMad/agents/dev.md` — MODIFIED: Added SSH access info

---

## QA Results
<!-- Results from QA Agent review of the completed story implementation -->
