# Shared Module Resources

Code and hardware components shared across all modules.

## Firmware

See `firmware/` for shared firmware libraries (I2C protocol, OTA, utilities).

## Hardware

See `hardware/` for shared KiCad libraries and common 3D parts.
